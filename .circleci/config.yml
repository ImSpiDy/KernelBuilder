version: 2.1
jobs:
  lav-KSU:
   docker:
      - image: nexgang/ubuntu:latest
   steps:
      - run:
          command: |
           git config --global user.name "ImSpiDy"
           git config --global user.email "SpiDy2713@gmail.com"
           git config --global color.ui false
           git clone --depth=1 https://ImSpiDy:$my_og_token@github.com/ImSpiDy/kernel_xiaomi_lavender-4.19 .
           bash build.sh

workflows:
  version: 2.1
  cooking:
    jobs:
      - lav-KSU
